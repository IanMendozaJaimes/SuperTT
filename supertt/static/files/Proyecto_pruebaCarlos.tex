\documentclass[12pt]{article}

%configuraciones del idioma
\usepackage[spanish]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{bookman}

%cosas de matematicas
\usepackage{amsmath, amsfonts, amssymb}

%dimensions
\usepackage[left=3.1cm, right=3.1cm, top=1.5cm]{geometry}

\begin{document}

{ \sqrt { 8 } ^ { { { s } + { { a } } ^ { 7 . 8 } } } } 

a = \frac { a ^ 2 + b ^ 2 - 4 a b \cos ^ 2 \theta } 

x ^ 5 = b x ^ 3 = e ^ 2 

x ^ { i } + c x + x + x } 

\frac { 8 n ^ 2 } { 8 , \frac { 1 } { a ^ 2 + \frac { 1 } { 5 6 x } } } = \frac { 2 a b } { a + b _ 0 } 

\frac { { \frac { d } { \frac { 1 } { a + \frac { 1 } { a } + \frac { 1 } { a } + \frac { 1 } { c } } } + x + \frac { 1 } { 4 } } + \frac { 1 } { \sqrt { 2 } } } 

a ^ { \alpha a ^ 2 } = - a ( a ) ^ t + x ^ { p + k } ^ { f ^ 2 } } 

\sqrt { x ^ { \frac { a ^ 2 } { x + \frac { x ^ 2 } } } } + x ^ { \frac { 0 } { x + \frac { 1 } } } } } 



\end{document}